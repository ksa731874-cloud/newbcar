[railway]
# Force Docker deployment - Railway will detect and use Dockerfile
dockerfilePath = "./Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckPeriod = "10s"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
