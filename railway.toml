[railway]
entrypoint = "php"

[nixpacks]
PHP_VERSION = "84"
PHP_EXTENSIONS = "pdo_mysql mysqli"

[deploy]
buildCommand = "docker build -t app . && docker run -p 8080:80 app"
healthcheckPath = "/"
healthcheckPeriod = "10s"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
